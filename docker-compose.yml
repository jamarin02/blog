version: '3.1'
services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: Test1ng
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  db-data:
    driver: local